
-- The game settings
battleship.settings = {}
local settings = battleship.settings

-- Output extra information into logs (Also instead of placing schematics we just output the command to the logs)
settings.debug = true
